# Nurture Pathway — the live app

This repository holds only the compiled web build of the Nurture Pathway app, published by the
`deploy` workflow of the private source repository `taylor585/nurture-pathway` after its gates pass.
Nothing is edited here by hand; every commit is a build, named by the source commit it came from.

Live: https://taylor585.github.io/nurture-pathway-app/
